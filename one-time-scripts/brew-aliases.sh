brew alias i='install --no-quarantine' # Reason: I trust Homebrew packages, and trust myself to not install malicious software.
brew alias uni='uninstall --zap' # Use the --force if this fails due to `.app` bundle already deleted.
